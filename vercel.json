{
  "version": 2,
  "builds": [
    {
      "src": "server.mjs",
      "use": "@vercel/node",
      "config": { "runtime": "nodejs22.x", "maxDuration": 60, "memory": 1024 }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.mjs" }
  ]
}
